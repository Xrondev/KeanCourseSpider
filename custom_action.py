class custom:
    def __init__(self):
        # do something here
        print('Do initialization here')
        pass

    def main(self, course_info):
        """
            在这里定义需要的操作逻辑，例如上传到数据库等.
            :param course_info: 传入的是每一条课程，形如{id, term, name, title, section, prof, days, time, description, comments, capacity, enrolled}
            :return:
            """
        # do something here
    def end(self):
        """
        操作结束时会调用该函数
        :return:
        """